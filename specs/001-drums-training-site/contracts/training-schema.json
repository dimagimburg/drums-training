{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "training-schema.json",
  "title": "Training Configuration",
  "description": "Schema for content/training.yaml. Defines the training routine as an ordered list of exercises. The kid does all exercises in sequence — the session length is the array length.",
  "type": "object",
  "required": ["exercises"],
  "properties": {
    "exercises": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "title", "lessons"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
            "description": "Unique identifier for this exercise."
          },
          "title": {
            "type": "string",
            "minLength": 1,
            "description": "Display title for the exercise."
          },
          "description": {
            "type": "string",
            "description": "Optional description or instructions."
          },
          "lessons": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "description": "Lesson ID referencing an entry in content/lessons.yaml."
            },
            "description": "List of lesson IDs the kid can pick from for this exercise. At least 1 required."
          }
        },
        "additionalProperties": false
      },
      "description": "Ordered list of exercises in the training routine. The kid completes all of them."
    }
  },
  "additionalProperties": false
}
